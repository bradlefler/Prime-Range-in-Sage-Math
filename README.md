# Prime-Range-in-Sage-Math

# This function was made using the Sieve of Eratosthenes to find all of the primes between two given numbers. This is meant for large numbers, so the output does not include every prime found.
